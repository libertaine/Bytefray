{
  "name": "v5_dual_team",
  "display": "V5 Dual Team (Starter)",
  "description": "V5 starter: two processes, a raider and a keeper, splitting one action quota and one shared contact memory.",
  "kind": "python",
  "api_version": 2,
  "entrypoint": "agent.py:create_agent",
  "version": "1.1.0",
  "parameters": {
    "raider_share": {
      "type": "number",
      "default": 0.5,
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "The raider's share of the entrant's eight actions per tick. The keeper receives the rest, so the two shares always total exactly 1.0. 0.5 gives each process four actions; 0.75 gives the raider six and the keeper two."
    }
  },
  "presets": {
    "standard": {
      "description": "The declared default: an even split, four actions each.",
      "values": {
        "raider_share": 0.5
      }
    },
    "raid_heavy": {
      "description": "Six actions to the raider, two to the keeper. More offensive pressure, a core that is refreshed far less often.",
      "values": {
        "raider_share": 0.75
      }
    }
  }
}
