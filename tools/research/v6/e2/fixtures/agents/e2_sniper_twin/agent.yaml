name: e2_sniper_twin
display: E2 Global Sniper (twin)
description: Research-only V6 E2 fixture (design review Sec G.2) - global-reach immediate core attacker (does E2 only delay the dominant attack?). Not a product starter agent.
kind: python
api_version: 2
entrypoint: agent.py:create_agent
version: "1.0.0"
