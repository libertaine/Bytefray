name: e2_min_guard
display: E2 Minimal Guard
description: Research-only V6 E2 fixture (design review Sec G.2) - disrupts, restores exactly one core cell, attacks with the rest (is one-cell reset too cheap?). Not a product starter agent.
kind: python
api_version: 2
entrypoint: agent.py:create_agent
version: "1.0.0"
